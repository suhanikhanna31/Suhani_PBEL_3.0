# Core data / numeric
pandas
numpy

# Visualization
matplotlib
seaborn

# NLP
# Primary: transformer-based sentiment (Hugging Face) + spaCy POS tagging —
# (commenting out to save space in free-tier deployment)
# transformers
# torch
# spacy

# en_core_web_sm isn't pip-installable by name; after `pip install -r requirements.txt` run:
#   python -m spacy download en_core_web_sm
textstat
# Fallbacks only — used automatically if transformers/spaCy models aren't available
# (e.g. no internet access to fetch weights). Safe to leave installed.
nltk
textblob

# ML - supervised & unsupervised
scikit-learn
xgboost
joblib
shap

# API
fastapi
uvicorn[standard]
pydantic
python-multipart

# Config / env
python-dotenv

# watsonx.ai SDK (optional — code degrades gracefully without it)
ibm-watsonx-ai

# Testing
pytest
httpx

# Notebooks
jupyter
